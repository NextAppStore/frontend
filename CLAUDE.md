# Click-n-Deploy Frontend

Die verbindlichen Arbeitsanweisungen für dieses Repository stehen in `AGENTS.md`.

Diese Datei enthält selbst **keine** Regeln — sie zieht `AGENTS.md` nur herein, damit Claude
Code den Projektkontext automatisch lädt. `AGENTS.md` bleibt die einzige inhaltliche Quelle
und funktioniert unabhängig davon auch für andere Agenten, die diese Konvention lesen.

Inhaltliche Änderungen gehören deshalb ausschließlich in `AGENTS.md`, nie hierher.

@AGENTS.md
